%h1 Listing expenses

%table{:class => "expenses"}
  %thead
    %tr
      %th Username
      %th Cost (Euro)
      %th Time period
      %th Paid for user
      %th 

  %tbody
    - @expenses.each do |expense|
      %tr
        %td= expense.user.name
        %td= expense.cost
        %td= expense.time_period.try(:strftime, "%B %Y")
        %td= expense.paid_for_user.try(:name)
        %td= link_to 'More information', expense
%br
= link_to 'Add a new expense', new_expense_path
%br
= link_to 'Overview of total balances', balances_path
