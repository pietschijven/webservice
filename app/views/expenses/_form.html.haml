= form_for(@expense) do |f|
  - if @expense.errors.any?
    #error_explanation
      %h2
        = pluralize(@expense.errors.count, "error")
        prohibited this expense from being saved:
      %ul
        - @expense.errors.full_messages.each do |message|
          %li= message
  .field
    = f.label "User name"
    %br
      = f.collection_select :user_id, User.order(:name), :id , :name
  .field
    = f.label :cost
    %br
      = f.text_field :cost
  .field
    = f.label :paid_for_user
    %br
      = f.collection_select :paid_for_user, User.order(:name), :id, :name, include_blank: true
  .actions
    = f.submit
