class ApiKey < ActiveRecord::Base
  before_create :generate_access_token
  
  private
  
  def generate_access_token
    begin
      self.acces_token = SecureRandom.hex
    end while self.class.exists?(acces_token: acces_token)
  end
end
